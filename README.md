# MKE Novel Drug-Like Molecules — 100 Compound Evaluation Preview

This repository hosts the official **100-molecule evaluation preview dataset** generated by the MKE Chemistry Platform. This structured sample allows computational chemists, data scientists, and machine learning researchers to test molecular syntax, verify valency grammar, and benchmark virtual screening models before commercial deployment.

## 📊 Sample Validation Profile

The complete unabridged library contains **22,154 novel structures** generated natively on local consumer hardware via an 80M-parameter Causal Transformer. The property distributions of this preview explicitly match the macro-level portfolio metrics:

- **Identity Novelty:** 100% verified non-overlapping against 4,643,595 known compounds across MOSES, ZINC-250k, and ChEMBL (v34).
- **Lipinski Rule of 5 Compliance:** 99.1% Pass Rate (Pre-optimized for oral bioavailability).
- **Average LogP (Solubility):** 1.53 (Ideal therapeutic window for human cell permeability).
- **Synthetic Accessibility:** SA Score capping at ≤ 4.0 (Average under 3.0; cheap and straightforward to synthesize).

## 📂 File Schema

**File:** `preview_100_molecules.csv`

| Column | Type | Description |
| :--- | :--- | :--- |
| `smiles` | string | Canonical SMILES (RDKit validated syntax) |
| `qed` | float | Quantitative Estimate of Drug-likeness (0–1 scale) |
| `sa` | float | Synthetic Accessibility score (1–10 scale, lower = easier) |
| `mw` | float | Molecular Weight (Da, hard-capped at ≤ 500 Da) |
| `logp` | float | Octanol-Water Partition Coefficient (Solubility/Lipophilicity) |

## 🚀 Quick Start (Python / RDKit Evaluation)

To instantly pull this preview file into your local environment for graph neural network (GNN) embeddings or structure-based docking simulations:

```bash
git clone https://github.com
cd mke-novel-druglike-molecules-preview
```

```python
import pandas as pd
from rdkit import Chem
from rdkit.Chem import Draw

# Load the MKE evaluation matrix
df = pd.read_csv("preview_100_molecules.csv")

# Sanity check RDKit C++ graph parsing natively
for smiles in df['smiles'].head(5):
    mol = Chem.MolFromSmiles(smiles)
    print(f"SMILES Validated: {Chem.MolToSmiles(mol)}")
```

## 💎 Full Unabridged Access & Commercial Licensing

For enterprise virtual screening operations, lead optimization pipelines, or industrial research teams who want to completely bypass compute overhead and instantly download the full **22,154 asset matrix**—complete with the pre-allocated ADMET property arrays, the pre-sorted 5,508-molecule Elite Core file, and a perpetual commercial use license:

👉 [**Purchase Full Dataset Access on Gumroad ($500) →**](https://mkechem.gumroad.com/)

*For evaluation benchmarks or alternative data formats, tracking updates are also available on our [Hugging Face Hub]([https://huggingface.co](https://huggingface.co/datasets/MKEChem/mke-novel-druglike-smiles)).*
